// Functions related to whitelist checking
function isSiteWhitelisted(whitelistedSites, currentSite) {
    return whitelistedSites.includes(currentSite.toLowerCase());
  }
  
  // Other whitelist-related functions...
  